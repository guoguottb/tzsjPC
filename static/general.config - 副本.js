var generalConfig =  {
   hasLayout: false, // 是否显示基础布局（头部、侧边栏等），默认为显示（true）
   hasSupportTip: false, // 是否显示技术支持信息，默认为显示（true）
   hasToggleAnimation: true, // 是否播放切换动画，默认播放（true）
   hasTopLayout:false,//是否显示头部菜单，默认为侧边导航（false）
   menuListName:'sub-menuList',  //菜单存入sessionSotre中的key名，不能与其他子应用重名，不然会覆盖
   appId:'',  //子应用的appId
}